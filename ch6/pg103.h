#ifndef GUARD_pg103_h
#define GUARD_pg103_h

#include <string>
#include <vector>
#include <algorithm>
#include <iterator>

bool space(char);
bool not_space(char);
std::vector<std::string> split(const std::string&);
bool is_palindrome(const std::string&);



#endif
